node_modules/
dist/
.DS_Store

# Yerel yapılandırma ve sırlar — asla commit edilmez
.env
.env.*
!.env.example
config.local.json

# Test/geliştirme çıktıları
outputs/
screenshots/
.qa/

# Kurulum paketi (build çıktısı)
package/
logs/
dist-guard

# Guard eklentisi — public depoya asla çıkmaz (kaynak + imzalı paket + özel anahtar)
guard/
tests/guard.test.js
tests/warmup.test.js
dist-guard/
dist-guard.crx
dist-guard.pem

# İmza anahtarları ve imzalı paketler
*.pem
*.crx
*.key

# Claude Code yerel worktree kopyaları (199 MB)
.claude/worktrees/

# Sahadan gelen kurulum ekleri: gerçek sunucu adları, IP'ler, sertifika parmak
# izleri ve hesap adları içerir; depoya girmez. Genel geçer olan kısımları
# server/scripts/KURULUM.md ve deploy/iis altına işlenir.
Redakt_Ekler/
